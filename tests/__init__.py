# Этот файл позволяет Python распознавать папку tests как пакет.
# Остаётся пустым, пока не используется специфическая логика импорта.

# Пример: можно в будущем импортировать тестовые модули через `tests.test_normalizer`
# или использовать unittest discovery.